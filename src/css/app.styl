// app global css
* {
    outline: none;
}
i {
    font-size: 32px;
}
.q-layout__section--marginal {
    background: #edf3f8;
    color:#000;
    padding 8px 8px;
}
.q-layout__shadow::after {
    box-shadow: none;
}
.q-toolbar__title {
    text-align: center;
    transform: translateX(-5%);
}

.q-page {
    min-height: auto !important;
}